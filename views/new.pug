extends layout

block content
  form(method="post", action="/transcriptions").my-3
    input(type="hidden" name="_csrf" value!=csrfToken)
    div.form-group
      label(for="transcriptionName") 原稿名
      input(type="text" name="transcriptionName")#transcriptionName.form-control
    button(type="submit").btn.btn-info 原稿をつくる