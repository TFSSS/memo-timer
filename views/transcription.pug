extends layout

block content
  h4.my-3 原稿
  p(style="white-space:pre;") 再生画面か編集画面へ
  div.container
    div.row
      a(href=`/transcriptions/${transcriptionId}/edit`,role="button").btn.btn-info 編集画面へ
    div.row
      a(href=`/transcriptions/${transcriptionId}/play`,role="button").btn.btn-info 再生画面へ